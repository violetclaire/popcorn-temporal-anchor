# Trial 3 interim results

Unseeded public search: 8 queries; target 767-2676.com not found. Participant selected Cloudflare Roughtime provisionally. No verifier ran (missing Go and initial network restriction).
Voluntary shared-board referral: created for Cloudflare Roughtime.
Recipient: returning participant independently inspected three pages and chose INVESTIGATE. No executable proof or adoption claimed.
Documentation-entry trial: in progress. Target Glama URL explicitly supplied. This is seeded exposure, not organic discovery; it asks whether an encountered listing leads to a runnable sample.
Root diagnostic public GET: listing HTTP 200, updated name and sample URL present. This does not imply search ranking or agent selection.

Documentation-entry transport update: child web/PowerShell/Node all failed (Node EACCES). Root retrieved the exact supplied public listing with Node, HTTP 200, 229584 bytes, and saved listing.html for the participant to inspect. This is explicitly assisted retrieval, not independent network access. Child chooses any onward documentation link.

Documentation-entry result: participant inspected and executed once, exit 0. verified=true; digest_matches=true; one_byte_who_tamper_rejected=true; historical_evidence=true; establishes_current_time=false; authorization_granted=false; payment_sent=false; network_request_made=false. Participant would use as an offline evaluation. Exact listing was supplied and root handled downloads after EACCES, so this establishes an assisted documentation-to-sample path, not organic discovery. Referral passed to returning recipient for separate inspection.

Recipient result: inspected the supplied public-source copy, independently ran once offline, exit 0; signature/digest passed and built-in one-byte mutation rejected. Decision: use for historical demonstration, investigate before operational use. Boundary evaluation helper was not tested by sample entrypoint. Target referral thus reached independently repeated offline verification after explicit initial listing exposure; no organic target discovery or wider external adoption established.
