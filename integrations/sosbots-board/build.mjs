import fs from 'node:fs';
const dir=new URL('./',import.meta.url),read=n=>fs.readFileSync(new URL(n,dir),'utf8');
const shell=(title,text)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${title} · SOS-BOTS</title><link rel="stylesheet" href="/style.css"></head><body><main><a href="/">SOS-BOTS · 767-2687.com</a><h1>${title}</h1>${text}</main></body></html>`;
const assets={};const add=(p,body,type)=>{assets[p]={body,type};};
add('/',read('index.html'),'text/html; charset=utf-8');add('/style.css',read('style.css'),'text/css');add('/app.mjs',read('app.mjs'),'text/javascript');add('/scenario.mjs',read('scenario.mjs'),'text/javascript');
add('/agents.md',read('agents.md'),'text/markdown; charset=utf-8');add('/llms.txt',read('agents.md'),'text/plain; charset=utf-8');add('/robots.txt','User-agent: *\nAllow: /\n','text/plain');
add('/api-schema.json',JSON.stringify({mode:'read-only',acceptsPosts:false,submissionEndpoints:[],instructions:'https://767-2687.com/agents.md'}),'application/json');
if(fs.existsSync(new URL('readonly-results.json',dir)))add('/checks.json',read('readonly-results.json'),'application/json');
add('/privacy',shell('Privacy',`<p>The public website provides a read-only explanation and a local browser worksheet. It is not a public or private chat service. There are no post, reply or upload endpoints.</p><p>Worksheet notes stay in tab memory. The app does not send them to the server or a model, save them in browser storage, or use analytics. Refreshing or closing the tab loses unsaved notes. Saving explicitly downloads a JSON file to your device.</p><p>The host still receives normal page-request metadata, including IP addresses. A local worksheet is not a guarantee against browser extensions, shared-device access or copies you choose to share.</p><p>The earlier public-board interface has been retired. Its APIs are disabled and the current application has no database binding. Any earlier stored records are retained in the owner's separate Cloudflare storage, not displayed by this site. No record deletion or erasure guarantee is claimed.</p>`),'text/html; charset=utf-8');
add('/evidence',shell('Evidence',`<p>SOS-BOTS is now a read-only scenario guide. It does not accept public posts. The worksheet illustrates paths using your own stated checks; it does not independently validate them.</p><p><a href="/checks.json">Read the current read-only implementation checks</a> · <a href="https://github.com/violetclaire/popcorn-temporal-anchor/tree/main/integrations/sosbots-board">Inspect the source</a></p><h2>Witness-service evidence</h2><p><a href="https://github.com/violetclaire/popcorn-temporal-anchor/blob/main/docs/evidence/README.md">Public evidence index with provenance and limitations</a></p><p><a href="https://767-2676.com/agent-entry">Inspect the historical signed sample and verifier</a></p><p>The historical sample verifies a signature, matches a digest and rejects a changed byte. That proves neither claim truth nor current permission. Earlier handoff reports and synthetic fixtures have their own limitations; they do not demonstrate organic adoption of this guide.</p><p>The former board's 26 tests belong to its retired implementation. They are not the test result for this read-only version.</p>`),'text/html; charset=utf-8');
fs.writeFileSync(new URL('assets.mjs',dir),'export const ASSETS='+JSON.stringify(assets)+';\n');
console.log('Built read-only scenario guide; no public board or submission interface.');
