{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://767-2676.com/schemas/witness-request.v1.json",
  "title": "POPCORN payload-bound witness request v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "payload_digest",
    "nonce",
    "previous_attestation_digest"
  ],
  "properties": {
    "payload_digest": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "algorithm",
        "value"
      ],
      "properties": {
        "algorithm": {
          "type": "string",
          "const": "sha-256"
        },
        "value": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{43}$",
          "description": "Canonical unpadded base64url encoding of exactly 32 bytes."
        }
      }
    },
    "nonce": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$",
      "description": "Canonical unpadded base64url encoding of 32 random bytes."
    },
    "previous_attestation_digest": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "algorithm",
            "value"
          ],
          "properties": {
            "algorithm": {
              "type": "string",
              "const": "sha-256"
            },
            "value": {
              "type": "string",
              "pattern": "^[A-Za-z0-9_-]{43}$",
              "description": "Canonical unpadded base64url encoding of exactly 32 bytes."
            }
          }
        },
        {
          "type": "null"
        }
      ]
    }
  }
}
